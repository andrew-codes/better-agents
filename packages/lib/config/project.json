{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "lib-config",
  "projectType": "library",
  "sourceRoot": "packages/lib/config/src",
  "tags": ["scope:lib"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/lib/config",
        "command": "tsc -p tsconfig.json"
      }
    }
  }
}
